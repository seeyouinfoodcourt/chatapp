import { StyleSheet } from 'react-native';

export const sharedStyles = StyleSheet.create({
    container: {
        flex: 1,
        // paddingHorizontal: 8,
        // borderColor: 'yellow',
        // borderWidth: 1,
    },
});
